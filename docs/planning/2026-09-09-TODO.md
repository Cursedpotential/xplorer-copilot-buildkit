# TODO — 2026-09-09

> _Byline: Claude Code · Fable 5.1 · 2026-09-09_

Kit-internal. Not copied into the fork.

## Orders from owner (this session)

- [x] `init`: create a kit-level `CLAUDE.md`; existing fork-facing file renamed to `CLAUDE.copilot.md`; README updated to match.
- [x] Record policy: personal tool, never push to upstream `kimlimjustin/xplorer`, upstream fetch-only, push only to own fork and own repo.
- [x] Create a remote for this kit: private GitHub repo `Cursedpotential/xplorer-copilot-buildkit` (origin), `git init` here, initial import pushed to `main`.
- [x] Fork `kimlimjustin/xplorer` → `Cursedpotential/xplorer` (public; GitHub forks of public repos cannot be private without detaching). Cloned to `../xplorer-copilot`, `upstream` push URL DISABLED, branch `feat/acp-copilot` off `next` at f59e6202.
- [ ] Next (not yet ordered): merge kit into the fork per README quick start, then run Phase 0 preflight (`pnpm install`, `pnpm run dev`, build gate).
- [x] Parent routers updated (owner order 01:57): `casebible/.gitignore` ignores `xplorer-copilot-buildkit/` (both children); `casebible/AGENTS.md` child-repositories table; workspace `Projects/AGENTS.md`, `Projects/REPOSITORY_BOUNDARIES.md`, `AGENT_MEMORY.md`, `.gitignore` annotated. Parent edits left uncommitted in the owner's checkout (explicit-path staging only).
- [x] Owner 02:05–02:08: commit + push parent-router edits (workspace `75a55355` → `origin/master`; stale 29h `index.lock` removed on owner's "fix all the errors"); commit the whole `casebible/` tree LOCALLY (no remote, stays local): `core.longpaths=true`, HANDOFF.md DSN password redacted, two `_intake` exports with private keys gitignored, staged set secret-scanned clean before commit.
- [x] Owner 03:17: `Projects/mitech-consult-site` must be untracked in the workspace (own repo + remote). Gitlink removed from parent index, path gitignored, router docs updated; workspace commit `187f1a2a` pushed. Child untouched.
- [ ] Owner 03:17 note: `xplorer-copilot` "will have its own repo and own remote" — already true as a GitHub fork (`origin` Cursedpotential/xplorer, public). If a standalone private repo is wanted instead of a fork, that is a separate order.
